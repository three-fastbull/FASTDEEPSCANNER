# ชุดคำสั่งสำหรับส่งให้ AI ติดตั้งให้

คัดลอกข้อความในกรอบด้านล่างทั้งหมด **แก้อีเมลบรรทัดแรกเป็นอีเมลของตัวเอง** แล้วส่งให้ AI

> **ใช้ได้กับ AI ที่สั่งงานเครื่องได้** เช่น Claude Code, Cursor, GitHub Copilot Agent
> ถ้าใช้แชทธรรมดา (ChatGPT หรือ Claude ในเว็บ) AI จะทำให้ไม่ได้ แต่จะอธิบายทีละขั้นให้แทน

---

## ข้อความที่ต้องคัดลอก

```
ช่วยติดตั้งโปรแกรม FastDeep Scanner บนเครื่องนี้ให้หน่อยครับ ทำให้จนเสร็จเลย

อีเมลของผม: ใส่อีเมลของคุณตรงนี้@gmail.com

== โปรแกรมนี้คืออะไร ==
เครื่องมือคัดกรองหุ้นเพื่อการศึกษา ทำงานบนเครื่องตัวเองล้วน ๆ ไม่ต้องสมัครสมาชิก
ไม่มีค่าใช้จ่าย ใช้ Python มาตรฐานอย่างเดียว ไม่ต้องติดตั้งไลบรารีเพิ่ม

โค้ด: https://github.com/three-fastbull/FASTDEEPSCANNER

== สิ่งที่ต้องทำ ==

1. ตรวจว่าเครื่องมี Python 3.11 ขึ้นไปไหม (สั่ง: python --version หรือ py -3 --version)
   ถ้าไม่มีหรือเก่ากว่านั้น ให้บอกผมว่าต้องไปโหลดจาก https://www.python.org/downloads/
   และย้ำว่าตอนติดตั้งต้องติ๊กช่อง "Add Python to PATH" ด้วย แล้วรอผมติดตั้งเสร็จก่อน

2. ดาวน์โหลดโค้ดจาก GitHub ข้างบน แล้วแตกไฟล์ไว้ในโฟลเดอร์ที่ไม่ซิงก์กับ OneDrive
   (เช่น D:\FASTDEEP SCANNER หรือ C:\FASTDEEP) เพราะข้อมูลจะโตถึง 600 MB
   ถ้าเครื่องมี git ใช้ git clone ได้ ถ้าไม่มีให้โหลดไฟล์ ZIP จาก
   https://github.com/three-fastbull/FASTDEEPSCANNER/archive/refs/heads/main.zip

3. เข้าไปในโฟลเดอร์นั้น แล้วสร้างไฟล์ชื่อ .env มีเนื้อหาสองบรรทัดนี้
   โดยใช้อีเมลที่ผมให้ไว้ข้างบน (ต้องเป็นอีเมลจริงที่รับเมลได้ ห้ามใช้ของคนอื่น
   เพราะ SEC ซึ่งเป็นแหล่งงบการเงินทางการของสหรัฐบังคับ และจะตอบ 403 ถ้าใช้อีเมลปลอม)

   FASTDEEP_SEC_CONTACT=อีเมลของผมที่ให้ไว้ข้างบน
   ANTHROPIC_API_KEY=

4. ดึงราคาหุ้น 1,458 ตัว ย้อนหลัง 5 ปี (ใช้เวลาประมาณ 2-5 นาที ได้ไฟล์ราว 136 MB)

   python -m fastdeep_scanner update-prices --universe data/fastdeep_universe.csv --out data/fastdeep_prices.csv --range 5y --interval 1d --workers 6

5. ดึงงบการเงินจาก SEC EDGAR (ใช้เวลาประมาณ 15 นาที ได้ไฟล์ราว 366 MB)

   python -m fastdeep_scanner update-sec-financials --universe data/fastdeep_universe.csv --cache-dir data/financial_cache --groups SP500,NASDAQ100,SP400

6. ตรวจว่าครบแล้วหรือยัง

   python -m fastdeep_scanner doctor

   คำสั่งนี้จะบอกเองว่าอะไรผ่านอะไรขาด และถ้าขาดจะบอกคำสั่งที่ใช้แก้มาให้เลย
   ถ้ายังไม่ผ่านครบให้แก้ตามที่มันบอก แล้วรันใหม่จนผ่านหมด

7. เปิดโปรแกรม

   python -m fastdeep_scanner serve --port 8765

   แล้วบอกผมให้เปิดเบราว์เซอร์ไปที่ http://127.0.0.1:8765
   (ครั้งแรกอาจใช้เวลา 5-10 วินาทีกว่าจะขึ้นครบ เพราะต้องอ่านไฟล์ราคา 136 MB)

== ถ้าเจอปัญหา ==

- "ไม่พบ Python" หรือ python ไม่ใช่คำสั่งที่รู้จัก
  แปลว่าตอนติดตั้ง Python ลืมติ๊ก Add Python to PATH ให้ลอง py -3 แทน
  ถ้ายังไม่ได้ ให้บอกผมไปติดตั้ง Python ใหม่

- ดึงราคาแล้วช้ามากหรือขึ้นว่าหมดเวลา
  Yahoo จำกัดอัตราชั่วคราว ไม่ใช่ความผิดของเรา
  ให้รอ 10-15 นาทีแล้วรันคำสั่งเดิมซ้ำ ระบบจะทำต่อจากเดิม ไม่ต้องเริ่มใหม่
  อย่าลองรัวถี่ ๆ เพราะจะยิ่งโดนจำกัดหนักขึ้น

- ดึงงบแล้วขึ้น 403
  แปลว่าอีเมลในไฟล์ .env ใช้ไม่ได้จริง ให้ตรวจว่าพิมพ์ถูกและเป็นอีเมลที่ใช้งานอยู่

- ถ้าดึงงบไม่ครบทุกตัว ไม่เป็นไร ใช้งานได้แล้ว รันคำสั่งข้อ 5 ซ้ำภายหลังเพื่อเก็บที่เหลือ

== ขอความกรุณา ==

- รันเฉพาะคำสั่งที่ระบุไว้ข้างบน อย่าติดตั้งอะไรเพิ่มเอง โปรแกรมนี้ไม่ต้องใช้ pip เลย
- ถ้าติดตรงไหนที่ต้องให้ผมตัดสินใจ ให้ถามผมก่อน อย่าเดา
- ทำเสร็จแล้วสรุปให้หน่อยว่าติดตั้งไว้ที่โฟลเดอร์ไหน และเปิดโปรแกรมครั้งต่อไปยังไง
```

---

## ทางลัดถ้าไม่อยากใช้ AI

ในโฟลเดอร์โปรแกรมมีไฟล์ `setup.bat` ทำทุกอย่างข้างบนให้อยู่แล้ว
ดับเบิลคลิกได้เลย หรือเปิด Command Prompt แล้วพิมพ์

```
setup.bat you@example.com
```

อ่านรายละเอียดเพิ่มที่ [README_นักเรียน.md](README_นักเรียน.md)
